---
phase: 01-foundation-core-learning-loop
plan: 05
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - frontend/src/components/FeedbackModal.jsx
  - frontend/src/components/FeedbackFAB.jsx
  - frontend/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "A feedback form is accessible to students (e.g., via a FAB or dedicated page)."
    - "Students can submit feedback (rating and comment) through this form."
    - "Submitted feedback is successfully sent to the backend API."
  artifacts:
    - path: "frontend/src/components/FeedbackModal.jsx"
      provides: "React component for submitting lesson feedback"
      min_lines: 40
    - path: "frontend/src/components/FeedbackFAB.jsx"
      provides: "Floating Action Button for easy feedback access"
      contains: "<FeedbackModal"
  key_links:
    - from: "frontend/src/components/FeedbackModal.jsx"
      to: "/api/feedback/"
      via: "POST request to submit feedback"
      pattern: "apiClient.post('/feedback')"
---

<objective>
Develop a frontend component for students to submit feedback and integrate it into the application (globally or within lesson context).

Purpose: To enable students to provide feedback on learning content, fulfilling the "FEED-01: Student feedback on content" requirement.
Output: React components `FeedbackModal.jsx` and `FeedbackFAB.jsx`.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference for frontend component creation and API interaction
@frontend/src/main.tsx
@frontend/src/App.tsx

# Depends on Plan 02 for the Feedback API
@.planning/phases/01-foundation-core-learning-loop/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create Feedback Modal Component</name>
  <files>frontend/src/components/FeedbackModal.jsx</files>
  <action>
    Implement `frontend/src/components/FeedbackModal.jsx` to collect and submit feedback.
  </action>
  <verify>
    Check `FeedbackModal.jsx` for the form structure and submission logic.
  </verify>
  <done>
    `FeedbackModal.jsx` is a functional component for collecting and submitting feedback.
  </done>
</task>

<task type="auto">
  <name>Integrate Feedback Access</name>
  <files>
    frontend/src/components/FeedbackFAB.jsx
    frontend/src/App.tsx
  </files>
  <action>
    Implement `FeedbackFAB.jsx` to trigger the `FeedbackModal`.
    Add `FeedbackFAB` to `App.tsx` for global access.
  </action>
  <verify>
    Verify that the feedback button is visible and opens the modal.
    Attempt to submit feedback.
  </verify>
  <done>
    The feedback form is accessible globally via a FAB.
  </done>
</task>

</tasks>


<verification>
After both tasks are complete, and with the backend (`01-02-PLAN.md`) running:
- Navigate to any lesson detail page.
- Fill out the feedback form (rating and comment) and submit.
- Verify that a success message appears (if implemented) or check the network tab for a successful POST request to `/api/feedback/` (HTTP 200/201).
</verification>

<success_criteria>
- The `FeedbackForm.vue` component is correctly implemented and integrated into `LessonDetail.vue`.
- Students can successfully submit feedback from the lesson detail page, and this feedback is received by the backend API.
- The user interface provides clear indication of feedback submission status (success/error).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-learning-loop/01-05-SUMMARY.md`
</output>
