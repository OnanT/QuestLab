---
phase: 01-foundation-core-learning-loop
plan: 05
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - frontend/src/components/FeedbackForm.vue
  - frontend/src/views/LessonDetail.vue
autonomous: true

must_haves:
  truths:
    - "A feedback form is visible on the lesson detail page."
    - "Students can submit feedback (rating and comment) through this form."
    - "Submitted feedback is successfully sent to the backend API."
  artifacts:
    - path: "frontend/src/components/FeedbackForm.vue"
      provides: "Vue component for submitting lesson feedback"
      min_lines: 40
    - path: "frontend/src/views/LessonDetail.vue"
      provides: "Integration of the feedback form component"
      contains: "<FeedbackForm"
  key_links:
    - from: "frontend/src/components/FeedbackForm.vue"
      to: "/api/feedback/"
      via: "POST request to submit feedback"
      pattern: "axios.post('.*api/feedback')"
---

<objective>
Develop a frontend component for students to submit feedback on a lesson and integrate it into the lesson detail view.

Purpose: To enable students to provide feedback on learning content, fulfilling the "FEED-01: Student feedback on content" requirement.
Output: A new Vue component `FeedbackForm.vue` and its integration into `LessonDetail.vue`.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference for frontend component creation and API interaction
@frontend/src/main.js
@frontend/src/api/index.js # Assumed API service or axios configuration
@frontend/src/views/LessonDetail.vue

# Depends on Plan 02 for the Feedback API
@.planning/phases/01-foundation-core-learning-loop/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create Feedback Submission Form Component</name>
  <files>frontend/src/components/FeedbackForm.vue</files>
  <action>
    Create `frontend/src/components/FeedbackForm.vue`. This component should:
    - Accept `lessonId` as a prop.
    - Include input fields for `rating` (e.g., a dropdown or radio buttons 1-5) and `comment` (a textarea).
    - Have a submit button.
    - On submission, send a POST request to `/api/feedback/` with the lesson ID, rating, and comment.
    - Handle success (e.g., show a success message) and error states.
    - The component should be designed to receive the `lessonId` from its parent, which will be the `LessonDetail.vue`.
  </action>
  <verify>
    Check `frontend/src/components/FeedbackForm.vue` for the form structure, input fields, and submission logic.
    Ensure `lessonId` prop is correctly defined and used in the API call.
  </verify>
  <done>
    `FeedbackForm.vue` is a standalone component capable of collecting and submitting feedback.
  </done>
</task>

<task type="auto">
  <name>Integrate Feedback Form into Lesson Detail</name>
  <files>frontend/src/views/LessonDetail.vue</files>
  <action>
    Open `frontend/src/views/LessonDetail.vue`.
    Import the `FeedbackForm.vue` component.
    Add the `FeedbackForm` component to the template, passing the current `lessonId` as a prop.
    Position the form clearly within the lesson detail page, for example, at the bottom of the content.
  </action>
  <verify>
    Navigate to a lesson detail page (`/lessons/:id`).
    Verify that the feedback form is visible on the page.
    Attempt to submit feedback (requires a running backend with API).
  </verify>
  <done>
    The feedback form is successfully integrated into the lesson detail page, allowing users to submit feedback.
  </done>
</task>

</tasks>

<verification>
After both tasks are complete, and with the backend (`01-02-PLAN.md`) running:
- Navigate to any lesson detail page.
- Fill out the feedback form (rating and comment) and submit.
- Verify that a success message appears (if implemented) or check the network tab for a successful POST request to `/api/feedback/` (HTTP 200/201).
</verification>

<success_criteria>
- The `FeedbackForm.vue` component is correctly implemented and integrated into `LessonDetail.vue`.
- Students can successfully submit feedback from the lesson detail page, and this feedback is received by the backend API.
- The user interface provides clear indication of feedback submission status (success/error).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-learning-loop/01-05-SUMMARY.md`
</output>
