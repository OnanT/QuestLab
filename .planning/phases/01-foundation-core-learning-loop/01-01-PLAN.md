---
phase: 01-foundation-core-learning-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/models.py
  - backend/schemas.py
autonomous: true

must_haves:
  truths:
    - "The `Feedback` model can store feedback linked to a user and a lesson."
    - "Feedback data can be serialized and deserialized via Pydantic schemas."
  artifacts:
    - path: "backend/models.py"
      provides: "Feedback model definition"
      contains: "class Feedback(Base):"
    - path: "backend/schemas.py"
      provides: "FeedbackCreate and FeedbackOut schemas"
      contains: "class FeedbackBase(BaseModel):"
  key_links:
    - from: "backend/models.py"
      to: "sqlalchemy.ext.declarative.declarative_base"
      via: "Inheritance from Base"
      pattern: "class Feedback(Base):"
    - from: "backend/models.py"
      to: "User model"
      via: "ForeignKey('users.id')"
      pattern: "ForeignKey('users.id')"
    - from: "backend/models.py"
      to: "Lesson model"
      via: "ForeignKey('lessons.id')"
      pattern: "ForeignKey('lessons.id')"
---

<objective>
Implement the `Feedback` model in `backend/models.py` and its corresponding Pydantic schemas in `backend/schemas.py`.

Purpose: To establish the database structure and data validation for capturing user feedback on lessons, which is a missing core component for the "FEED-01: Student feedback on content" requirement.
Output: Modified `backend/models.py` and `backend/schemas.py` files with the new `Feedback` model and schemas.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-core-learning-loop/01-01-PLAN.md

# Reference for existing models and schemas structure
@backend/models.py
@backend/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Add Feedback Model to models.py</name>
  <files>backend/models.py</files>
  <action>
    Add a new SQLAlchemy model `Feedback` to `backend/models.py`.
    It should include:
    - `id`: Primary key (Integer)
    - `user_id`: ForeignKey to `users.id` (Integer, nullable=False)
    - `lesson_id`: ForeignKey to `lessons.id` (Integer, nullable=False)
    - `rating`: Integer (e.g., 1-5, nullable=False)
    - `comment`: Text (nullable=True)
    - `created_at`: TIMESTAMP with `server_default=text("CURRENT_TIMESTAMP")`
    - Relationships: `user = relationship("User", back_populates="feedback_given")` and `lesson = relationship("Lesson", back_populates="feedback_received")`.
    Update the `User` model to add `feedback_given = relationship("Feedback", back_populates="user")`.
    Update the `Lesson` model to add `feedback_received = relationship("Feedback", back_populates="lesson")`.
  </action>
  <verify>
    Check `backend/models.py` for the new `Feedback` class and updated relationships in `User` and `Lesson`.
    Ensure no syntax errors exist.
  </verify>
  <done>
    `backend/models.py` contains the `Feedback` model and its relationships are correctly defined.
  </done>
</task>

<task type="auto">
  <name>Add Feedback Schemas to schemas.py</name>
  <files>backend/schemas.py</files>
  <action>
    Add Pydantic schemas for `Feedback` to `backend/schemas.py`.
    - `FeedbackBase`: with `user_id`, `lesson_id`, `rating`, `comment`.
    - `FeedbackCreate`: inherits from `FeedbackBase`.
    - `FeedbackOut`: inherits from `FeedbackBase`, adds `id`, `created_at`.
    Ensure proper type hints and `BaseModel` inheritance.
  </action>
  <verify>
    Check `backend/schemas.py` for `FeedbackBase`, `FeedbackCreate`, and `FeedbackOut` classes.
    Ensure they match the model's fields and data types.
  </verify>
  <done>
    `backend/schemas.py` contains the Pydantic schemas required for `Feedback` API operations.
  </done>
</task>

</tasks>

<verification>
After both tasks are complete, the `Feedback` model and schemas should be correctly defined, ready for API implementation. A manual inspection of the `models.py` and `schemas.py` files should confirm the additions and correct relationships/typing.
</verification>

<success_criteria>
- `backend/models.py` successfully defines the `Feedback` SQLAlchemy model with `user_id`, `lesson_id`, `rating`, `comment`, and `created_at` fields, and the reciprocal relationships in `User` and `Lesson` models.
- `backend/schemas.py` successfully defines `FeedbackBase`, `FeedbackCreate`, and `FeedbackOut` Pydantic schemas corresponding to the `Feedback` model fields.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-learning-loop/01-01-SUMMARY.md`
</output>
