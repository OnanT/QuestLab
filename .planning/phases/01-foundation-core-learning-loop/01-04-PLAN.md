---
phase: 01-foundation-core-learning-loop
plan: 04
type: execute
wave: 3
depends_on: [] # Assumes backend lesson and progress APIs are already functional
files_modified:
  - frontend/src/views/LessonList.vue
  - frontend/src/views/LessonDetail.vue
  - frontend/src/components/LessonProgress.vue
  - frontend/src/router/index.js # or equivalent router configuration
autonomous: true

must_haves:
  truths:
    - "The student dashboard displays a list of available lessons (quests)."
    - "Clicking on a lesson from the list navigates to its detail view."
    - "The lesson detail view shows the content of the lesson and the student's progress if available."
  artifacts:
    - path: "frontend/src/views/LessonList.vue"
      provides: "Vue component for listing lessons"
      min_lines: 50
    - path: "frontend/src/views/LessonDetail.vue"
      provides: "Vue component for displaying lesson content"
      min_lines: 70
    - path: "frontend/src/components/LessonProgress.vue"
      provides: "Vue component for showing lesson progress"
      min_lines: 30
    - path: "frontend/src/router/index.js"
      provides: "Routing configuration for lesson list and detail"
      contains: "/lessons"
  key_links:
    - from: "frontend/src/views/LessonList.vue"
      to: "/api/lessons/"
      via: "API call to fetch lesson list"
      pattern: "axios.get('.*api/lessons')"
    - from: "frontend/src/views/LessonDetail.vue"
      to: "/api/lessons/{id}"
      via: "API call to fetch lesson details"
      pattern: "axios.get('.*api/lessons/')"
    - from: "frontend/src/views/LessonDetail.vue"
      to: "/api/progress/user/{user_id}/lesson/{lesson_id}"
      via: "API call to fetch lesson progress"
      pattern: "axios.get('.*api/progress/')"
---

<objective>
Develop the core student-facing UI components for viewing a list of available lessons (quests), displaying individual lesson content, and showing the student's progress for completed lessons.

Purpose: To enable students to engage with learning content, fulfilling the "LEARN-01: Engage with content presented as quests" and contributing to "PROG-02: Show progress to students" requirements.
Output: New Vue components for lesson listing, detail, and progress display, along with updated routing configuration.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference for frontend structure and API interaction patterns
@frontend/src/main.js
@frontend/src/router/index.js
@frontend/src/api/index.js # Assumed API service or axios configuration
</context>

<tasks>

<task type="auto">
  <name>Create Lesson Listing Component and Route</name>
  <files>
    frontend/src/views/LessonList.vue
    frontend/src/router/index.js
  </files>
  <action>
    Create `frontend/src/views/LessonList.vue`. This component should:
    - Fetch a list of lessons from `/api/lessons/` when mounted.
    - Display each lesson's title and a brief description.
    - Provide a link or button for each lesson to navigate to its detail view (`/lessons/:id`).
    Update `frontend/src/router/index.js` to add a route for `/lessons` that uses `LessonList.vue`.
  </action>
  <verify>
    Ensure `LessonList.vue` fetches and displays lesson data.
    Verify that the `/lessons` route is accessible and displays the component.
  </verify>
  <done>
    A functional lesson list page is available at `/lessons`.
  </done>
</task>

<task type="auto">
  <name>Create Lesson Detail Component and Route</name>
  <files>
    frontend/src/views/LessonDetail.vue
    frontend/src/router/index.js
  </files>
  <action>
    Create `frontend/src/views/LessonDetail.vue`. This component should:
    - Accept a lesson ID as a route parameter.
    - Fetch the specific lesson's details from `/api/lessons/:id` when mounted.
    - Display the lesson's title, description, and content (e.g., `content_html`).
    - Integrate a placeholder for future lesson interactions (e.g., "Start Lesson" button).
    Update `frontend/src/router/index.js` to add a dynamic route for `/lessons/:id` that uses `LessonDetail.vue`.
  </action>
  <verify>
    Ensure `LessonDetail.vue` fetches and displays the content for a specific lesson ID.
    Verify that ` /lessons/:id` routes are working and display the lesson.
  </verify>
  <done>
    A functional lesson detail page is available at `/lessons/:id`.
  </done>
</task>

<task type="auto">
  <name>Integrate Lesson Progress Display</name>
  <files>
    frontend/src/components/LessonProgress.vue
    frontend/src/views/LessonDetail.vue
  </files>
  <action>
    Create `frontend/src/components/LessonProgress.vue`. This component should:
    - Accept `lessonId` and `userId` as props.
    - Fetch the user's progress for the given lesson from `/api/progress/user/:userId/lesson/:lessonId`.
    - Display a simple progress indicator (e.g., "Completed", "Score: X%").
    Integrate `LessonProgress.vue` into `frontend/src/views/LessonDetail.vue`, passing the current lesson's ID and the authenticated user's ID.
  </action>
  <verify>
    On the `LessonDetail.vue` page, confirm that the `LessonProgress.vue` component is rendered.
    If test data for progress exists, verify that progress information is fetched and displayed.
  </verify>
  <done>
    Lesson detail page includes a visible progress display for the current user.
  </done>
</task>

</tasks>

<verification>
After all tasks are complete:
- Navigate to `/lessons` and verify that a list of lessons is displayed.
- Click on a lesson and verify that its details are displayed at `/lessons/:id`.
- Verify that the progress component is visible on the lesson detail page, showing accurate (or placeholder) progress.
</verification>

<success_criteria>
- The `LessonList.vue` component successfully fetches and renders a list of lessons.
- The `LessonDetail.vue` component successfully fetches and renders content for a specific lesson ID.
- The `LessonProgress.vue` component is integrated into `LessonDetail.vue` and attempts to fetch and display progress data.
- Frontend routing for `/lessons` and `/lessons/:id` is correctly configured and functional.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-learning-loop/01-04-SUMMARY.md`
</output>
