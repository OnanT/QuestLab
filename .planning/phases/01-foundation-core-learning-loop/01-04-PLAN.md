---
phase: 01-foundation-core-learning-loop
plan: 04
type: execute
wave: 3
depends_on: []
files_modified:
  - frontend/src/pages/LessonsPage.jsx
  - frontend/src/pages/LessonsViewPage.jsx
  - frontend/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "The student dashboard displays a list of available lessons."
    - "Clicking on a lesson navigates to its detail view."
    - "The lesson detail view shows content and handles student progress."
  artifacts:
    - path: "frontend/src/pages/LessonsPage.jsx"
      provides: "React page for listing lessons"
      min_lines: 50
    - path: "frontend/src/pages/LessonsViewPage.jsx"
      provides: "React page for displaying lesson content and progress"
      min_lines: 70
    - path: "frontend/src/App.tsx"
      provides: "Routing configuration"
      contains: "/lessons"
---

<objective>
Develop the core student-facing UI components for viewing a list of available lessons (quests), displaying individual lesson content, and showing/updating the student's progress.

Purpose: To enable students to engage with learning content, fulfilling the "LEARN-01: Engage with content presented as quests" and contributing to "PROG-02: Show progress to students" requirements.
Output: React components for lesson listing, detail, and progress display.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference for frontend structure and API interaction patterns
@frontend/src/main.tsx
@frontend/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Create Lesson Listing Page and Route</name>
  <files>
    frontend/src/pages/LessonsPage.jsx
    frontend/src/App.tsx
  </files>
  <action>
    Implement `frontend/src/pages/LessonsPage.jsx` to fetch and display lessons from `/api/lessons`.
    Update `frontend/src/App.tsx` to add the route for `/lessons`.
  </action>
  <verify>
    Ensure `LessonsPage.jsx` fetches and displays lesson data.
    Verify that the `/lessons` route is accessible and displays the component.
  </verify>
  <done>
    A functional lesson list page is available at `/lessons`.
  </done>
</task>

<task type="auto">
  <name>Create Lesson View Page and Route</name>
  <files>
    frontend/src/pages/LessonsViewPage.jsx
    frontend/src/App.tsx
  </files>
  <action>
    Implement `frontend/src/pages/LessonsViewPage.jsx` to display lesson content and manage user progress.
    Update `frontend/src/App.tsx` to add the route for `/lessons/:lessonId`.
  </action>
  <verify>
    Ensure `LessonsViewPage.jsx` fetches and displays the content for a specific lesson ID.
    Verify that `/lessons/:id` routes are working and display the lesson.
  </verify>
  <done>
    A functional lesson detail page is available at `/lessons/:id`.
  </done>
</task>

</tasks>


<verification>
After all tasks are complete:
- Navigate to `/lessons` and verify that a list of lessons is displayed.
- Click on a lesson and verify that its details are displayed at `/lessons/:id`.
- Verify that the progress component is visible on the lesson detail page, showing accurate (or placeholder) progress.
</verification>

<success_criteria>
- The `LessonList.vue` component successfully fetches and renders a list of lessons.
- The `LessonDetail.vue` component successfully fetches and renders content for a specific lesson ID.
- The `LessonProgress.vue` component is integrated into `LessonDetail.vue` and attempts to fetch and display progress data.
- Frontend routing for `/lessons` and `/lessons/:id` is correctly configured and functional.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-learning-loop/01-04-SUMMARY.md`
</output>
