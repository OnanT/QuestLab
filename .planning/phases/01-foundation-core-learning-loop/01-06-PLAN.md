---
phase: 01-foundation-core-learning-loop
plan: 06
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - frontend/src/pages/AdminDashboard.jsx
  - frontend/src/pages/admin/AdminLessons.jsx
  - frontend/src/pages/admin/AdminSubjects.jsx
  - frontend/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Authenticated admin users can access an 'Admin Dashboard' route."
    - "The admin dashboard contains navigation to manage lessons and subjects."
    - "Users can perform CRUD operations on lessons and subjects."
  artifacts:
    - path: "frontend/src/pages/AdminDashboard.jsx"
      provides: "Entry point for admin management UI"
      min_lines: 30
    - path: "frontend/src/pages/admin/AdminLessons.jsx"
      provides: "UI for lesson CRUD operations"
      min_lines: 70
    - path: "frontend/src/pages/admin/AdminSubjects.jsx"
      provides: "UI for subject CRUD operations"
      min_lines: 70
    - path: "frontend/src/App.tsx"
      provides: "Routing configuration for admin routes"
      contains: "/admin"
---

<objective>
Develop frontend components for admins to manage lessons and subjects, including viewing lists and performing CRUD operations.

Purpose: To provide the interface for managing learning content and curriculum structure, fulfilling "ADMIN-02" and "ADMIN-03" requirements.
Output: React components for admin management.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference for frontend structure, API interaction, and existing components
@frontend/src/main.tsx
@frontend/src/App.tsx

# Depends on Plan 03 for RBAC protected API endpoints
@.planning/phases/01-foundation-core-learning-loop/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create Admin Dashboard and Router Entry</name>
  <files>
    frontend/src/pages/AdminDashboard.jsx
    frontend/src/App.tsx
  </files>
  <action>
    Implement `frontend/src/pages/AdminDashboard.jsx` as the entry point for admin functionalities.
    Update `frontend/src/App.tsx` to add the route for `/admin/*`.
  </action>
  <verify>
    Verify that the `/admin` route is accessible to admins and displays the dashboard.
  </verify>
  <done>
    An admin dashboard entry point and route are configured.
  </done>
</task>

<task type="auto">
  <name>Create Admin Lesson Management UI</name>
  <files>
    frontend/src/pages/admin/AdminLessons.jsx
    frontend/src/pages/AdminDashboard.jsx
  </files>
  <action>
    Implement `frontend/src/pages/admin/AdminLessons.jsx` for lesson CRUD operations.
    Register it as a sub-route in `AdminDashboard.jsx`.
  </action>
  <verify>
    Navigate to `/admin/lessons` and verify CRUD functionality.
  </verify>
  <done>
    A UI for managing lessons (CRUD) is implemented.
  </done>
</task>

<task type="auto">
  <name>Create Admin Subject Management UI</name>
  <files>
    frontend/src/pages/admin/AdminSubjects.jsx
    frontend/src/pages/AdminDashboard.jsx
  </files>
  <action>
    Implement `frontend/src/pages/admin/AdminSubjects.jsx` for subject CRUD operations.
    Register it as a sub-route in `AdminDashboard.jsx`.
  </action>
  <verify>
    Navigate to `/admin/subjects` and verify CRUD functionality.
  </verify>
  <done>
    A UI for managing subjects (CRUD) is implemented.
  </done>
</task>

</tasks>


<verification>
After all tasks are complete, and with the backend (`01-03-PLAN.md`) running:
- Authenticate as an 'admin' or 'teacher' user.
- Navigate to `/admin`. Verify the dashboard and links to lesson/subject management.
- Navigate to `/admin/lessons` and perform CRUD operations on lessons. Verify success.
- Navigate to `/admin/subjects` and perform CRUD operations on subjects. Verify success.
- Authenticate as a 'student' user and attempt to access `/admin/lessons` or `/admin/subjects`. Verify access is denied (frontend protection, if implemented, or backend 403).
</verification>

<success_criteria>
- `AdminDashboard.vue`, `AdminLessonManagement.vue`, and `AdminSubjectManagement.vue` are created and correctly routed.
- The admin UI allows for viewing, creating, editing, and deleting lessons and subjects.
- Backend RBAC (from plan 03) successfully protects these admin operations, ensuring only authorized users can perform them.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-learning-loop/01-06-SUMMARY.md`
</output>
