---
phase: 01-foundation-core-learning-loop
plan: 06
type: execute
wave: 4
depends_on: ["01-03"] # Depends on RBAC being in place for protected admin APIs
files_modified:
  - frontend/src/views/AdminDashboard.vue
  - frontend/src/views/AdminLessonManagement.vue
  - frontend/src/views/AdminSubjectManagement.vue
  - frontend/src/router/index.js # or equivalent router configuration
autonomous: true

must_haves:
  truths:
    - "Authenticated admin/teacher users can access an 'Admin Dashboard' route."
    - "The admin dashboard contains links or navigation to manage lessons and subjects."
    - "Users can view a list of lessons and subjects within the admin interface."
    - "Users can create, edit, and delete lessons and subjects through the admin UI, protected by backend RBAC."
  artifacts:
    - path: "frontend/src/views/AdminDashboard.vue"
      provides: "Entry point for admin management UI"
      min_lines: 30
    - path: "frontend/src/views/AdminLessonManagement.vue"
      provides: "UI for lesson CRUD operations"
      min_lines: 70
    - path: "frontend/src/views/AdminSubjectManagement.vue"
      provides: "UI for subject CRUD operations"
      min_lines: 70
    - path: "frontend/src/router/index.js"
      provides: "Routing configuration for admin routes"
      contains: "/admin"
  key_links:
    - from: "frontend/src/views/AdminLessonManagement.vue"
      to: "/api/lessons/"
      via: "API calls for lesson CRUD"
      pattern: "axios.(get|post|put|delete)('.*api/lessons')"
    - from: "frontend/src/views/AdminSubjectManagement.vue"
      to: "/api/subjects/"
      via: "API calls for subject CRUD"
      pattern: "axios.(get|post|put|delete)('.*api/subjects')"
---

<objective>
Develop basic frontend components for teachers/admins to manage lessons and subjects, including viewing lists, and performing create, update, and delete (CRUD) operations.

Purpose: To provide the necessary interface for managing learning content and curriculum structure, fulfilling the "ADMIN-02: Manage content" and "ADMIN-03: Manage curriculum structure" requirements, and utilizing the RBAC from plan 03.
Output: New Vue components for `AdminDashboard`, `AdminLessonManagement`, `AdminSubjectManagement`, and updated routing configuration.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference for frontend structure, API interaction, and existing components
@frontend/src/main.js
@frontend/src/router/index.js
@frontend/src/api/index.js # Assumed API service or axios configuration
@frontend/src/views/LessonList.vue # For visual consistency

# Depends on Plan 03 for RBAC protected API endpoints
@.planning/phases/01-foundation-core-learning-loop/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create Admin Dashboard and Router Entry</name>
  <files>
    frontend/src/views/AdminDashboard.vue
    frontend/src/router/index.js
  </files>
  <action>
    Create `frontend/src/views/AdminDashboard.vue`. This component should:
    - Serve as the main entry point for admin functionalities.
    - Display navigation links to "Manage Lessons" and "Manage Subjects".
    Update `frontend/src/router/index.js` to add a route for `/admin` that uses `AdminDashboard.vue`. This route should ideally be protected so only 'admin' or 'teacher' roles can access it. (Note: Actual frontend protection logic will be handled by the framework, for this task focus on route creation).
  </action>
  <verify>
    Verify that the `/admin` route is accessible (after authentication) and displays the `AdminDashboard.vue` with navigation links.
  </verify>
  <done>
    An admin dashboard entry point and route are configured.
  </done>
</task>

<task type="auto">
  <name>Create Admin Lesson Management UI</name>
  <files>
    frontend/src/views/AdminLessonManagement.vue
    frontend/src/router/index.js
  </files>
  <action>
    Create `frontend/src/views/AdminLessonManagement.vue`. This component should:
    - Display a table or list of existing lessons, fetched from `/api/lessons/`.
    - Provide buttons or forms to:
      - Create a new lesson (POST to `/api/lessons/`)
      - Edit an existing lesson (PUT to `/api/lessons/:id`)
      - Delete a lesson (DELETE to `/api/lessons/:id`)
    Update `frontend/src/router/index.js` to add a route like `/admin/lessons` that uses `AdminLessonManagement.vue`.
  </action>
  <verify>
    Navigate to `/admin/lessons` and verify that a list of lessons is displayed.
    Visually confirm the presence of Create, Edit, Delete functionalities.
  </verify>
  <done>
    A UI for managing lessons (CRUD) is implemented and accessible via its route.
  </done>
</task>

<task type="auto">
  <name>Create Admin Subject Management UI</name>
  <files>
    frontend/src/views/AdminSubjectManagement.vue
    frontend/src/router/index.js
  </files>
  <action>
    Create `frontend/src/views/AdminSubjectManagement.vue`. This component should:
    - Display a table or list of existing subjects, fetched from `/api/subjects/`.
    - Provide buttons or forms to:
      - Create a new subject (POST to `/api/subjects/`)
      - Edit an existing subject (PUT to `/api/subjects/:id`)
      - Delete a subject (DELETE to `/api/subjects/:id`)
    Update `frontend/src/router/index.js` to add a route like `/admin/subjects` that uses `AdminSubjectManagement.vue`.
  </action>
  <verify>
    Navigate to `/admin/subjects` and verify that a list of subjects is displayed.
    Visually confirm the presence of Create, Edit, Delete functionalities.
  </verify>
  <done>
    A UI for managing subjects (CRUD) is implemented and accessible via its route.
  </done>
</task>

</tasks>

<verification>
After all tasks are complete, and with the backend (`01-03-PLAN.md`) running:
- Authenticate as an 'admin' or 'teacher' user.
- Navigate to `/admin`. Verify the dashboard and links to lesson/subject management.
- Navigate to `/admin/lessons` and perform CRUD operations on lessons. Verify success.
- Navigate to `/admin/subjects` and perform CRUD operations on subjects. Verify success.
- Authenticate as a 'student' user and attempt to access `/admin/lessons` or `/admin/subjects`. Verify access is denied (frontend protection, if implemented, or backend 403).
</verification>

<success_criteria>
- `AdminDashboard.vue`, `AdminLessonManagement.vue`, and `AdminSubjectManagement.vue` are created and correctly routed.
- The admin UI allows for viewing, creating, editing, and deleting lessons and subjects.
- Backend RBAC (from plan 03) successfully protects these admin operations, ensuring only authorized users can perform them.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-learning-loop/01-06-SUMMARY.md`
</output>
